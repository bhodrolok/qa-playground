# QA Automation Playground

Stack (so far):

- Selenium WebDriver — UI Automation layer -> Interface for web-browser UI automation
- Cucumber — Behavior (BDD) layer with Gherkin feature files --> Scenario Outlines
- TestNG — Test execution + management + assertion layer
- Maven Surefire — Build + Continous Integration (CI) layer

Trying to compartmentalize into multiple 'themes' each with their own README, setup, tests, etc.

1. clinic-management web app 
- Located in the [clinic](./clinic/) folder 
